<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.nexusenroll</groupId>
  <artifactId>enrollent-service</artifactId>
  <version>1.0-SNAPSHOT</version>
  <build>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.compiler.version}</version>
      </plugin>
      <plugin>
        <groupId>io.smallrye</groupId>
        <artifactId>jandex-maven-plugin</artifactId>
        <version>${jandex.maven.version}</version>
        <executions>
          <execution>
            <id>make-index</id>
            <goals>
              <goal>jandex</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${maven.shade.version}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer>
                  <mainClass>${mainClass}</mainClass>
                </transformer>
                <transformer />
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <dependencies>
    <dependency>
      <groupId>org.jboss</groupId>
      <artifactId>jandex</artifactId>
      <version>3.1.2</version>
      <scope>compile</scope>
    </dependency>
  </dependencies>
  <properties>
    <mainClass>com.nexusenroll.enrollment.EnrollmentApplication</mainClass>
    <jersey.version>3.1.5</jersey.version>
    <jakarta.ws.rs.version>3.1.0</jakarta.ws.rs.version>
    <jandex.maven.version>3.1.2</jandex.maven.version>
    <jandex.version>3.1.2</jandex.version>
    <jakarta.persistence.version>3.1.0</jakarta.persistence.version>
    <jjwt.version>0.11.5</jjwt.version>
    <hibernate.version>6.2.7.Final</hibernate.version>
    <maven.compiler.version>3.10.1</maven.compiler.version>
    <maven.shade.version>3.5.1</maven.shade.version>
    <maven.compiler.source>17</maven.compiler.source>
    <jakarta.cdi.version>4.0.1</jakarta.cdi.version>
    <maven.compiler.target>17</maven.compiler.target>
    <mysql.version>8.1.0</mysql.version>
  </properties>
</project>
